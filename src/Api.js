const BASE_API = '';

export default {
    checkToken: async (token) => {
    },
    signIn: async (email, password) => {
    },
    signUp: async (nome, email, password, rua, bairro, cidade, estado, genero) => {
    }
};